<%= form_for(@events_sponser) do |f| %>
  <% if @events_sponser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@events_sponser.errors.count, "error") %> prohibited this events_sponser from being saved:</h2>

      <ul>
      <% @events_sponser.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.number_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :sponser_id %><br>
    <%= f.number_field :sponser_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
